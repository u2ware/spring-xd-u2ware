<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:int="http://www.springframework.org/schema/integration"
	   xmlns:int-u2ware-bacnet="http://u2ware.github.io/schema/integration/bacnet"
	   xsi:schemaLocation="
	    http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration
		http://www.springframework.org/schema/integration/spring-integration.xsd
		http://u2ware.github.io/schema/integration/bacnet
		http://u2ware.github.io/schema/integration/bacnet/spring-integration-bacnet.xsd">

	<int:channel id="input"/>

	<int:channel id="output"/>
	
	<int-u2ware-bacnet:outbound-gateway id="bacnetOutboundGateway" 
		localPort="${localPort}"
		request-channel="bacnetRequest"
		reply-channel="bacnetResponse"/>

	<beans profile="use_json_input">
		<int:payload-type-router input-channel="input">
		    <int:mapping type="java.lang.String" channel="jsonRequest" />
		    <int:mapping type="io.github.u2ware.integration.bacnet.core.BacnetRequest" channel="bacnetRequest" />
		</int:payload-type-router>
	
		<int:json-to-object-transformer 
			input-channel="jsonRequest" 
			output-channel="bacnetRequest"
	    	type="io.github.u2ware.integration.bacnet.core.BacnetRequest"/>
	</beans>

	<beans profile="dont_use_json_input">
		<int:bridge input-channel="input" output-channel="bacnetRequest"/>
	</beans>

	
	<beans profile="use_splitter">
		<int:splitter input-channel="bacnetResponse" output-channel="outputReady"/>
	</beans>
	<beans profile="dont_use_splitter">
		<int:bridge input-channel="bacnetResponse" output-channel="outputReady"/>
	</beans>

	<beans profile="use_json_output">
		<int:object-to-json-transformer input-channel="outputReady" output-channel="output" 
						content-type="application/json" />
	</beans>
	<beans profile="dont_use_json_output">
		<int:bridge input-channel="outputReady" output-channel="output"/>
	</beans>
	
</beans>