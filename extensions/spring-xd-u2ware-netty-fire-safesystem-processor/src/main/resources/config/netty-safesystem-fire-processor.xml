<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:int="http://www.springframework.org/schema/integration"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration 
		http://www.springframework.org/schema/integration/spring-integration.xsd">
	
	<int:channel id="input"/>

	<int:channel id="output"/>
	
	<int:channel id="nettyRequest">
		<int:queue/>
	</int:channel>
	
	<int:channel id="nettyResponse"/>

	<bean class="io.github.u2ware.integration.netty.x.SafesystemFireClient">
		<property name="host" value="${host}"/>
		<property name="port" value="${port}"/>
		<property name="messageKeep" value="${messageKeep:false}"/>
		<property name="receiveChannel" ref="nettyRequest"/>
		<property name="sendChannel" ref="nettyResponse"/>
	</bean>

	<beans profile="use_json_input">
		<int:payload-type-router input-channel="input">
		    <int:mapping type="java.lang.String" channel="jsonRequest" />
		    <int:mapping type="io.github.u2ware.integration.netty.x.SafesystemFireRequest" channel="nettyRequest" />
		</int:payload-type-router>
	
		<int:json-to-object-transformer 
			input-channel="jsonRequest" 
			output-channel="nettyRequest"
	    	type="io.github.u2ware.integration.netty.x.SafesystemFireRequest"/>
	</beans>
	<beans profile="dont_use_json_input">
		<int:bridge input-channel="input" output-channel="nettyRequest"/>
	</beans>

	<beans profile="use_json_output">
		<int:object-to-json-transformer input-channel="nettyResponse" output-channel="output" content-type="application/json" />
	</beans>
	<beans profile="dont_use_json_output">
		<int:bridge input-channel="nettyResponse" output-channel="output"/>
	</beans>


</beans>