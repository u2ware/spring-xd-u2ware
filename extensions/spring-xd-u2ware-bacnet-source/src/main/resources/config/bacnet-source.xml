<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:int="http://www.springframework.org/schema/integration"
	   xmlns:int-u2ware-bacnet="http://u2ware.github.io/schema/integration/bacnet"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration
		http://www.springframework.org/schema/integration/spring-integration.xsd
		http://u2ware.github.io/schema/integration/bacnet
		http://u2ware.github.io/schema/integration/bacnet/spring-integration-bacnet.xsd">

	<int:channel id="output"/>

	<bean id="bacnetRequest" class="io.github.u2ware.integration.bacnet.inbound.BacnetRequestSupport">
		<constructor-arg value="${requestSupport}"/>
	</bean>

	<int-u2ware-bacnet:inbound-channel-adapter id="bacnetInboundChannelAdapter" 
		localPort="${localPort:47808}"
		request-support="bacnetRequest"

		channel="bacnetResponse">
			<int:poller fixed-rate="${fixedDelay}"/>
	</int-u2ware-bacnet:inbound-channel-adapter>


	<beans profile="use_splitter">
		<int:splitter input-channel="bacnetResponse" output-channel="outputResponse"/>
	</beans>
	<beans profile="dont_use_splitter">
		<int:bridge input-channel="bacnetResponse" output-channel="outputResponse"/>
	</beans>

	<beans profile="use_json_output">
		<int:object-to-json-transformer input-channel="outputResponse" output-channel="output" content-type="application/json" />
	</beans>
	<beans profile="dont_use_json_output">
		<int:bridge input-channel="outputResponse" output-channel="output"/>
	</beans>
</beans>