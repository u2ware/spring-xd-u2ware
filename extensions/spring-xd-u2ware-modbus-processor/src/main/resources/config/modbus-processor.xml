<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:int-u2ware-modbus="http://u2ware.github.io/schema/integration/modbus"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration 
		http://www.springframework.org/schema/integration/spring-integration.xsd
		http://u2ware.github.io/schema/integration/modbus 
		http://u2ware.github.io/schema/integration/modbus/spring-integration-modbus.xsd">

	<int:channel id="input"/>

	<int:channel id="output"/>


	<int-u2ware-modbus:outbound-gateway id="modbusOutboundGateway" 
			host="${host}"
			port="${port}"
			request-channel="modbusRequest"
			reply-channel="modbusResponse"/>

	<beans profile="use_json_input">
		<int:payload-type-router input-channel="input">
		    <int:mapping type="java.lang.String" channel="jsonRequest" />
		    <int:mapping type="io.github.u2ware.integration.modbus.core.ModbusRequest" channel="modbusRequest" />
		</int:payload-type-router>
	
		<int:json-to-object-transformer input-channel="jsonRequest" output-channel="modbusRequest"
	    	type="io.github.u2ware.integration.modbus.core.ModbusRequest"/>
	</beans>
	<beans profile="dont_use_json_input">
		<int:bridge input-channel="input" output-channel="modbusRequest"/>
	</beans>

	
	<beans profile="use_splitter">
		<int:splitter input-channel="modbusResponse" output-channel="outputReady"/>
	</beans>
	<beans profile="dont_use_splitter">
		<int:bridge input-channel="modbusResponse" output-channel="outputReady"/>
	</beans>

	<beans profile="use_json_output">
		<int:object-to-json-transformer input-channel="outputReady" output-channel="output" 
						content-type="application/json" />
	</beans>
	<beans profile="dont_use_json_output">
		<int:bridge input-channel="outputReady" output-channel="output"/>
	</beans>
</beans>