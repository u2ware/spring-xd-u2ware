<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:int-u2ware-snmp="http://u2ware.github.io/schema/integration/snmp"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration 
		http://www.springframework.org/schema/integration/spring-integration.xsd
		http://u2ware.github.io/schema/integration/snmp 
		http://u2ware.github.io/schema/integration/snmp/spring-integration-snmp.xsd">

	<int:channel id="input"/>

	<int:channel id="output"/>
	
	<int-u2ware-snmp:outbound-gateway id="snmpOutboundGateway" 
		local-port="${localPort:10162}"
		mib-file="${mibFile: }"
		request-channel="snmpRequest"
		reply-channel="snmpResponse"/>

	<beans profile="use_json_input">
		<int:payload-type-router input-channel="input">
		    <int:mapping type="java.lang.String" channel="jsonRequest" />
		    <int:mapping type="io.github.u2ware.integration.snmp.core.SnmpRequest" channel="snmpRequest" />
		</int:payload-type-router>
	
		<int:json-to-object-transformer 
			input-channel="jsonRequest" 
			output-channel="snmpRequest"
	    	type="io.github.u2ware.integration.snmp.core.SnmpRequest"/>
	</beans>

	<beans profile="dont_use_json_input">
		<int:bridge input-channel="input" output-channel="snmpRequest"/>
	</beans>

	
	<beans profile="use_splitter">
		<int:splitter input-channel="snmpResponse" output-channel="outputResponse"/>
	</beans>
	<beans profile="dont_use_splitter">
		<int:bridge input-channel="snmpResponse" output-channel="outputResponse"/>
	</beans>

	<beans profile="use_json_output">
		<int:object-to-json-transformer input-channel="outputResponse" output-channel="output" 
						content-type="application/json" />
	</beans>
	<beans profile="dont_use_json_output">
		<int:bridge input-channel="outputResponse" output-channel="output"/>
	</beans>
	
</beans>