<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:int="http://www.springframework.org/schema/integration"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration 
		http://www.springframework.org/schema/integration/spring-integration.xsd">
	

	<bean id="httpServer" class="io.github.u2ware.integration.netty.support.HttpMessageChannelAdapter">
		<property name="port" value="${httpPort}"/>
		<property name="sendChannel" ref="httpRequest"/>
		<property name="receiveChannel" ref="httpResponse"/>
		<property name="messagingTimeout" value="${httpTimeout}"/>
	</bean>

	<int:channel id="httpRequest"/>

	<int:json-to-object-transformer 
		input-channel="httpRequest" 
		output-channel="elevatorRequest"
    	type="io.github.u2ware.xd.netty.HyundaiElevatorRequest"/>

	<int:channel id="elevatorRequest">
		<int:queue/>
	</int:channel>

	<bean class="io.github.u2ware.xd.netty.HyundaiElevatorChannelAdapter">
		<property name="host" value="${host}"/>
		<property name="port" value="${port}"/>
		<property name="idleTimeout" value="0"/>
		<property name="receiveChannel" ref="elevatorRequest"/>
		<property name="sendChannel" ref="elevatorResponse"/>
	</bean>

	<int:channel id="elevatorResponse"/>


	<int:object-to-json-transformer 
		input-channel="elevatorResponse" 
		output-channel="httpResponse" content-type="application/json" />

	<int:channel id="httpResponse">
		<int:queue/>
	</int:channel>
		
</beans>