<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:int="http://www.springframework.org/schema/integration"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration 
		http://www.springframework.org/schema/integration/spring-integration.xsd">
	
	<int:channel id="output"/>
	
	<bean class="io.github.u2ware.xd.netty.HyundaiElevatorChannelAdapter">
		<property name="host" value="${host}"/>
		<property name="port" value="${port}"/>
		<property name="idleTimeout" value="${idleTimeout:3000}"/>
		<property name="sendChannel" ref="nettyResponse"/>
	</bean>
	
	<beans profile="use_splitter">
		<int:splitter input-channel="nettyResponse" output-channel="outputResponse"/>
	</beans>
	<beans profile="dont_use_splitter">
		<int:bridge input-channel="nettyResponse" output-channel="outputResponse"/>
	</beans>

	<beans profile="use_json_output">
		<int:object-to-json-transformer input-channel="outputResponse" output-channel="output" content-type="application/json" />
	</beans>
	<beans profile="dont_use_json_output">
		<int:bridge input-channel="outputResponse" output-channel="output"/>
	</beans>
	
</beans>